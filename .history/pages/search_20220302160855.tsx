import { useState, useEffect, FC } from "react";

const Search: FC = () => {
  const [state, setState] = useState(null);

  useEffect(() => {}, []); // on mount

  return <>hi</>;
};

export default Search;
